import { colors } from "./colors";

export const elevation = {
  card: {
    borderWidth: 1,
    borderColor: colors.border,
  },

  outline: {
    borderWidth: 1,
    borderColor: colors.border,
  },
} as const;
