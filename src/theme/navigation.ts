import { colors } from "./colors";

export const navigation = {
  tabBarHeight: 72,

  activeTint: colors.action,

  inactiveTint: colors.textMuted,

  background: colors.background,

  borderColor: colors.border,
} as const;
