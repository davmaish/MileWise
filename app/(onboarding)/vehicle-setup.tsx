import { View, Text } from 'react-native';

export default function VehicleSetupScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Mandatory Vehicle Onboarding Setup</Text>
    </View>
  );
}
